package CursoTodoCode;

public class OpeLogicos {
    public static void operLogicos(){
        //NOTA: Los operadores logicos e luna foma de convinar varios oepradores de realcion atraves de '&&' '||' y '!'
        //tambien retorna un valor boleano
        boolean resul;

        //OPERADORE LOGICO && 'AND', todods las sentencias teien que ser verdaderas para dar valor true.
        resul = (34>= 23) && (34 <= 100);
        resul = ('p' == 'p') && (34 <= 100);

        //OPERADOR LOGICO OR '||',, basta con que uno sea verdadero para que ejecute esta condicion
        resul = (34>= 23) || (34 <= 100);
        resul = (34>= 23) || (34 <= 100);



    }
}
